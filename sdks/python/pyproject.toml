[build-system]
requires = ["maturin>=0.12,<0.13"]
build-backend = "maturin"

[project]
name = "portscanner"
requires-python = ">=3.8"
dependencies = ["cffi"]

[tool.maturin]
bindings = "cffi"
sdist-include = ["target/header.h"]